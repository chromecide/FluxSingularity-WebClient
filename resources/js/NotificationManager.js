Ext.ns('FS.services');
Ext.define('FSManager.NotificationManager', {
	extend: Ext.util.Observable,
	pop: function(title, message, click_callback){
		
	}
},
function(){
	FS.services.NotificationManager = new FSManager.NotificationManager();
});